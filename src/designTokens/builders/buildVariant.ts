import type { VariantInput } from './types'
import { buildPrimitives } from './buildPrimitives'
import { buildSemantic } from './buildSemantic'
import { buildContextual } from './buildContextual'

export function buildVariant({ colors, contextual, semanticValues, additionalContextual, spacingTokens, extraPrimitives }: VariantInput): string {
	const lines = [
		'/*',
		' * Generated from src/designTokens.',
		' * Do not edit this file manually.',
		' */',
		'',
		...buildPrimitives(colors, extraPrimitives),
		...buildSemantic(semanticValues),
		...buildContextual(contextual, additionalContextual, spacingTokens),
		'',
	]

	return `${lines.join('\n')}\n`
}
