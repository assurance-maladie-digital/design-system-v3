import { Controls, Canvas, Meta, Source } from '@storybook/blocks';
import * as HijriCalendarStories from './HijriCalendar.stories';

<Meta of={HijriCalendarStories} />

<div className="header">
  <h1>HijriCalendar</h1>
  <p>Le composant `HijriCalendar` permet la saisie de dates selon le calendrier hijri (calendrier islamique) avec validation automatique et formatage.</p>
  <p>Il hérite des props et fonctionnalités du composant `SyTextField` de Synapse.</p>
</div>

<Canvas of={HijriCalendarStories.Default} />

# API

<Controls of={HijriCalendarStories.Default} />

# Exemples d'utilisation

## Utilisation de base

<Canvas of={HijriCalendarStories.Default} />

Saisie libre d'une date hijri sans contraintes particulières.

## Avec contraintes d'années

<Canvas of={HijriCalendarStories.WithYearConstraints} />

Limitation de la saisie entre deux années spécifiques.

## Avec année minimale seulement

<Canvas of={HijriCalendarStories.WithMinYearOnly} />

## Avec année maximale seulement

<Canvas of={HijriCalendarStories.WithMaxYearOnly} />

