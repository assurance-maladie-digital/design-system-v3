import { Controls, Canvas, Meta, Source } from '@storybook/addon-docs/blocks';
import * as MonthPicker from './MonthPicker.stories';

<Meta of={MonthPicker} />

<div className="header">
  <h1>MonthPicker</h1>
  <p>L’élément `MonthPicker` est utilisé pour sélectionner un mois et une année. Il est composé d'un champ de saisie et d'un sélecteur visuel.</p>
</div>

{/* func-version-start */}
<p className="func-version-badge">Dernière mise à jour fonctionnelle : V1.1.2 - 27/07/2026</p>
{/* func-version-end */}
{/* a11y-version-start */}
<p className="a11y-version-badge">Dernière mise à jour accessibilité : V1.0.23 - 25/03/2026</p>
{/* a11y-version-end */}


<Canvas
    of={MonthPicker.Default}
/>

# API

<Controls of={MonthPicker.Default} />

# Exemple d'utilisation

<Source dark code={`
<script setup lang="ts">
    import { MonthPicker } from '@cnamts/synapse'
    import { ref } from 'vue'

    const selectedMonth = ref<string | undefined>(undefined) // format: MM/YYYY
</script>

<template>
    <MonthPicker
        label="Début du projet"
        v-model="selectedMonth"
    />
</template>
`} />