import { Controls, Canvas, Meta, Source } from '@storybook/blocks';
import * as LunarCalendarStories from './LunarCalendar.stories';

<Meta of={LunarCalendarStories} />

<div className="header">
  <h1>LunarCalendar</h1>
  <p>Le composant `LunarCalendar` permet la saisie de dates selon le calendrier lunaire avec validation automatique et formatage.</p>
  <p>Il hérite des props et fonctionnalités du composant `SyTextField` de Synapse.</p>
</div>

{/* a11y-version-start */}
<p className="a11y-version-badge">Dernière mise à jour accessibilité : V1.0.22 - 30/03/2026</p>
{/* a11y-version-end */}


<Canvas of={LunarCalendarStories.Default} />

# API

<Controls of={LunarCalendarStories.Default} />

# Exemples d'utilisation

## Champ requis

<Canvas of={LunarCalendarStories.Required} />

## Avec bouton de suppression

<Canvas of={LunarCalendarStories.WithClearable} />

## Avec texte d'aide

<Canvas of={LunarCalendarStories.HelpText} />
