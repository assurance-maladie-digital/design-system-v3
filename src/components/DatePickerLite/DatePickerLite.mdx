import { Meta, Canvas, Controls, Source } from '@storybook/addon-docs/blocks'
import * as DatePickerLiteStories from './DatePickerLite.stories.ts'
import DatePickerLite from './DatePickerLite.vue'

<Meta title="Composants/Formulaires/DatePickerLite" component={DatePickerLite} />

<div className="header">
  <h1>DatePickerLite</h1>
  <p>Le composant DatePickerLite permet de sélectionner une date via un champ de saisie et un sélecteur visuel. Il prend en charge la navigation par mois et par année, la saisie manuelle, la validation et les interactions clavier.</p>
</div>

{/* func-version-start */}
<p className="func-version-badge">Dernière mise à jour fonctionnelle : V1.1.3 - 04/09/2026</p>
{/* func-version-end */}
{/* a11y-version-start */}
<p className="a11y-version-badge">Dernière mise à jour accessibilité : V1.1.3 - 04/09/2026</p>
{/* a11y-version-end */}

<Canvas of={DatePickerLiteStories.Default} />

# API

<Controls of={DatePickerLiteStories.Default} />

# Exemple d'utilisation

<Source dark code={`
<script setup lang="ts">
import { DatePickerLite } from '@cnamts/synapse'
import { ref } from 'vue'

const selectedDate = ref<Date | undefined>(new Date(2025, 10, 11))
</script>

<template>
  <DatePickerLite
    v-model="selectedDate"
    label="Début du projet"
    :min-year="2000"
    :max-year="2035"
    :clearable="true"
    required
  />
</template>
`} />

<a href="/?path=/docs/guide-du-dev-formulaires-validation-guide-des-formulaires--docs" className="action-link">Pour plus d'informations sur la validation, consultez le guide de validation des formulaires.</a>
