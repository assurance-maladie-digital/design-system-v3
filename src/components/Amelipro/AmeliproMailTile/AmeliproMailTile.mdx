import { Controls, Canvas, Meta, Source } from '@storybook/blocks';

import * as AmeliproMailTileStories from './AmeliproMailTile.stories';

<Meta of={AmeliproMailTileStories} />

# AmeliproMailTile

L’élément `AmeliproMailTile` est utilisé pour afficher tous les boutons/liens de type tuile résumé de mail existants dans la charte Amelipro

<Canvas of={AmeliproMailTileStories.Default} />

## API

<Controls of={AmeliproMailTileStories.Default} />
