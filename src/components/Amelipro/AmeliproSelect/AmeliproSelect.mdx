import { Controls, Canvas, Meta } from '@storybook/blocks';

import * as AmeliproSelectStories from './AmeliproSelect.stories';

<Meta of={AmeliproSelectStories} />

# AmeliproSelect

L’élément `AmeliproSelect` est utilisé pour permettre à l’utilisateur de sélectionner une option parmi un ensemble de choix.

<Canvas of={AmeliproSelectStories.Default} />

## API

<Controls of={AmeliproSelectStories.Default} />
