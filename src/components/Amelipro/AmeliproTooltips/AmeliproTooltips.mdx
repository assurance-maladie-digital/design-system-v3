import { Controls, Canvas, Meta } from '@storybook/blocks';

import * as AmeliproTooltipsStories from './AmeliproTooltips.stories';

<Meta of={AmeliproTooltipsStories} />

# AmeliproTooltips

L’élément `AmeliproTooltips` est utilisé pour afficher une infobulle sur un élément.

<Canvas of={AmeliproTooltipsStories.Default} />

## API

<Controls of={AmeliproTooltipsStories.Default} />
