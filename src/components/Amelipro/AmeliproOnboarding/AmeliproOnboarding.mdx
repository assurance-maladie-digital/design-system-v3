import { Controls, Canvas, Meta } from '@storybook/blocks';

import * as AmeliproOnboardingStories from './AmeliproOnboarding.stories';

<Meta of={AmeliproOnboardingStories} />

# AmeliproOnboarding

L’élément `AmeliproOnboarding` est utilisé pour afficher les fenêtres de dialogue contenant des tutoriels.

<Canvas of={AmeliproOnboardingStories.Default} />

## API

<Controls of={AmeliproOnboardingStories.Default} />
