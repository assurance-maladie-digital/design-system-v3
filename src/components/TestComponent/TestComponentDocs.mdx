import { Meta, ArgTypes } from '@storybook/blocks';
import * as TestComponentStories from './TestComponent.stories';

<Meta of={TestComponentStories} name="TestComponent" />

# Ceci est la page de documentation du composant `TestComponent`

Ce composant est présent à des fin de test et ne doit pas être utilisé en production.
<br/>

## API
<ArgTypes of={TestComponentStories} />
