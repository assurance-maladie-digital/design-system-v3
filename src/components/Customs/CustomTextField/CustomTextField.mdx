import { Meta, Canvas, Controls, Source } from '@storybook/blocks';
import * as CustomTextFieldStories from "./CustomTextField.stories";

<Meta of={CustomTextFieldStories} />

# CustomTextField

Le composant `CustomTextField` est utilisé pour proposer une alternative au `v-textArea` de Vuetify, conforme au style du design-system et qui  respecte les règles d'accessibilité RGAA.

<Canvas of={CustomTextFieldStories.Default} />


# API

<Controls of={CustomTextFieldStories.Default} />


# Exemple d'utilisation

<Source
    dark code={`
<script setup lang="ts">
	import { ref } from 'vue'
	import CustomSelect from '@cnamts/CustomTextField'

	const model = ref('')

</script>

<template>
	<main class="ma-12">
		Text saisie:	{{ model }}
		<div class="d-flex flex-wrap align-center">
        <CustomTextField
				v-model="model"
				prepend-icon="success"
				append-icon="close"
				preprend-inner-icon="info"
				show-divider
				append-inner-icon="success"
				variant-style="outlined"
				color="warning"
				is-clearable
				title="champs de prenom"
				aria-label="champs de nom"
				label="Label"
			/>
		</div>
	</main>
</template>
`}
/>
