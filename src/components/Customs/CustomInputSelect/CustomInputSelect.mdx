import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as CustomInputSelectStories from "./CustomInputSelect.stories.ts";

<Meta of={CustomInputSelectStories} />

# CustomSelect

Le composant `CustomInputSelect` est utilisé pour proposer une alternative au v-select de Vuetify qui ne respecte pas les regles RGAA.

# CustomSelect

<Canvas of={CustomInputSelectStories.Default} />

>Format des items :
>
> - Si les items passes en props sont des objets, il est possible de personnaliser les champs `text` et `value`  via les props `textKey` et `valueKey` (cf ci-dessous).
> - Si les items sont un tableau de string, le composant les utilisera directement.



# API

<Controls of={CustomInputSelectStories.Default} />