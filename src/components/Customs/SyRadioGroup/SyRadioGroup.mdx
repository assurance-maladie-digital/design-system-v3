import { Controls, Canvas, Meta, Source } from '@storybook/blocks';

import * as SyRadioGroupStories from './SyRadioGroup.stories';

<Meta of={SyRadioGroupStories} />

# SyRadioGroup

L’élément `SyRadioGroup` est utilisé lorsque vous voulez donner la possibilité à l’utilisateur de sélectionner une seule option d’un ensemble ou lorsqu’il est important pour lui d’avoir toutes les options visibles.

<Canvas of={SyRadioGroupStories.Default}  />

## API

<Controls of={SyRadioGroupStories.Default} />
