// optimisation à venir trés prochainement

// import { useTheme } from 'vuetify'

// const vuetifyTheme = useTheme()
// const theme = vuetifyTheme.name.value

// export const currentTheme = (locales: string): string => {
// 	return (theme === 'cnam' || theme === 'ap')
// 		? `${locales}.${theme}`
// 		: `${locales}.default`
// }
